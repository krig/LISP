{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb-dap",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceRoot}/komplodin",
            "args": ["tests/nums.scm"],
            "env": [],
            "cwd": "${workspaceRoot}"
        }
    ]
}
